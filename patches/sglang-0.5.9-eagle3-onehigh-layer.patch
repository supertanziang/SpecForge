--- /tmp/sglang_orig/sglang/srt/model_executor/cuda_graph_runner.py	2026-02-23 08:13:52.000000000 -0800
+++ /prj/corp/crd/morpheus/lasvegas/china-scratch/ziantan/minianaconda3/envs/specforge/lib/python3.11/site-packages/sglang/srt/model_executor/cuda_graph_runner.py	2026-05-25 00:04:45.494487000 -0700
@@ -362,7 +362,9 @@
             model_runner.spec_algorithm.is_eagle3()
             and model_runner.eagle_use_aux_hidden_state
         ):
-            self.model_runner.model.set_eagle3_layers_to_capture()
+            self.model_runner.model.set_eagle3_layers_to_capture(
+                getattr(model_runner, "eagle_aux_hidden_state_layer_ids", None)
+            )
 
         # Capture
         try:
--- /tmp/sglang_orig/sglang/srt/model_executor/model_runner.py	2026-02-23 08:13:52.000000000 -0800
+++ /prj/corp/crd/morpheus/lasvegas/china-scratch/ziantan/minianaconda3/envs/specforge/lib/python3.11/site-packages/sglang/srt/model_executor/model_runner.py	2026-05-25 00:04:53.327684000 -0700
@@ -1903,7 +1903,9 @@
             set_torch_compile_config()
 
         if self.eagle_use_aux_hidden_state:
-            self.model.set_eagle3_layers_to_capture()
+            self.model.set_eagle3_layers_to_capture(
+                getattr(self, "eagle_aux_hidden_state_layer_ids", None)
+            )
 
         require_mlp_tp_gather_ = require_mlp_tp_gather(self.server_args)
         if require_gathered_buffer(self.server_args):
--- /tmp/sglang_orig/sglang/srt/speculative/eagle_worker.py	2026-02-23 08:13:52.000000000 -0800
+++ /prj/corp/crd/morpheus/lasvegas/china-scratch/ziantan/minianaconda3/envs/specforge/lib/python3.11/site-packages/sglang/srt/speculative/eagle_worker.py	2026-05-25 00:06:17.528491000 -0700
@@ -190,6 +190,7 @@
             draft_tp_context if server_args.enable_dp_attention else empty_context
         )
         self.eagle_use_aux_hidden_state = False
+        self.eagle_aux_num_layers = 3
         if self.speculative_algorithm.is_eagle3():
             self.eagle_use_aux_hidden_state = True
             eagle_config = getattr(
@@ -198,6 +199,9 @@
             self.eagle_use_aux_hidden_state = eagle_config.get(
                 "use_aux_hidden_state", True
             )
+            layer_ids = eagle_config.get("eagle_aux_hidden_state_layer_ids", None)
+            if layer_ids is not None:
+                self.eagle_aux_num_layers = len(layer_ids)
         with self.draft_tp_context(
             self.draft_model_runner.tp_group
         ), speculative_moe_backend_context(), speculative_moe_a2a_backend_context():
@@ -914,7 +918,7 @@
             batch = batch.copy()
             batch.prepare_for_idle()
             hidden_size = (
-                self.model_config.hidden_size * 3
+                self.model_config.hidden_size * self.eagle_aux_num_layers
                 if self.speculative_algorithm.is_eagle3()
                 and self.eagle_use_aux_hidden_state
                 else self.model_config.hidden_size
--- /tmp/sglang_orig/sglang/srt/speculative/eagle_draft_extend_cuda_graph_runner.py	2026-02-23 08:13:52.000000000 -0800
+++ /prj/corp/crd/morpheus/lasvegas/china-scratch/ziantan/minianaconda3/envs/specforge/lib/python3.11/site-packages/sglang/srt/speculative/eagle_draft_extend_cuda_graph_runner.py	2026-05-25 00:09:54.407521000 -0700
@@ -104,17 +104,18 @@
                 self.eagle_worker.speculative_algorithm.is_eagle3()
                 and self.eagle_worker.eagle_use_aux_hidden_state
             ):
+                _aux_num_layers = getattr(self.eagle_worker, "eagle_aux_num_layers", 3)
                 self.hidden_states = torch.zeros(
                     (
                         self.max_num_token,
                         (
                             self.model_runner.model_config.hf_config.target_hidden_size
-                            * 3
+                            * _aux_num_layers
                             if hasattr(
                                 self.model_runner.model_config.hf_config,
                                 "target_hidden_size",
                             )
-                            else self.model_runner.model_config.hidden_size * 3
+                            else self.model_runner.model_config.hidden_size * _aux_num_layers
                         ),
                     ),
                     dtype=self.model_runner.dtype,
